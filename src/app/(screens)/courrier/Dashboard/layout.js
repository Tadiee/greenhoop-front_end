import CourrierHeader from "@/components/global components/header/courrierHeader";

export default function CourrierDashboardLayout({ children }) {
  return (
    <>
      {/* <CourrierHeader /> */}
      {children}
    </>
  );
}